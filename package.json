{
  "name": "vrchat-fallback-manager",
  "version": "0.3.0",
  "description": "VRChat fallback avatar manager",
  "main": "src/main.js",
  "license": "MIT",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "package": "electron-forge package && node scripts/remove-locales.js",
    "make": "electron-forge make",
    "build:installer": "npm run package && installer\\build-installer.bat"
  },
  "devDependencies": {
    "@electron-forge/cli": "^7.11.2",
    "electron": "^37.3.0"
  },
  "config": {
    "forge": "./forge.config.js"
  }
}